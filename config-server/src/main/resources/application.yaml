spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: /Users/geraldoluiz/Development/backend/microservices-ecommerce/config-data
          clone-on-start: true
          default-label: main
server:
  port: 8088